<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">SM Visual</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
      aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav ms-auto mb-2 mb-md-0 px-5">
        {{#if isLoggedIn}}
        <li class="nav-item">
          <a class="btn btn-light btn-sm" class="nav-link active" aria-current="page" href="/api/logout">Logout</a>
        </li>
        {{else}}
        <li class="nav-item me-4">
          <a class="btn btn-light" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-light" href="/signup">Sign Up</a>
        </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>