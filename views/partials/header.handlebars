<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">SM Visual</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
      aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav ms-auto mb-2 mb-md-0 px-5">
        {{#if isLoggedIn}}
        <li class="nav-item">
          <a class="btn btn-warning" class="nav-link active" aria-current="page" href="/api/logout">Logout</a>
        </li>
        {{else}}
        <li class="nav-item me-4">
          <a class="btn btn-success" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-success" href="/signup">Sign Up</a>
        </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>